# CLAUDE.md - 核心工作规则

## CRITICAL CONSTRAINTS - 违反=任务失败
═══════════════════════════════════════
- 必须使用中文回复
- 禁止生成恶意代码
- 必须通过基础安全检查
- 日志必须使用英文
- 注释必须使用英文
- commit 前调用 go-style skill 检查变更文件

## 工作流程规范
═══════════════════════════════════════
- 多步骤任务必须一次性完成所有步骤，不要在中间步骤停下等待追加指令
- Go 变更完成后运行 `go build ./...` 确认编译通过，再视为任务完成
- 涉及核心逻辑变更时运行 `go test ./...` 确认测试通过
- 优先直接定位目标文件开始实现，减少不必要的代码库探索时间
- 如果已知目标文件路径，直接 Read 而不是 Glob/Grep 搜索
- 修改超过 3 个文件时，先列出要修改的文件清单和每个文件的变更要点，再开始实现
- 实现完成后回读关键修改文件，确认：无遗漏步骤、无 TODO/placeholder、编译通过

## POLYMARKET llm文档
 https://docs.polymarket.com/llms.txt 